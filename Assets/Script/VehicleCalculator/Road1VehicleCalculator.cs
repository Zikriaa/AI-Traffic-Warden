using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Road1VehicleCalculator : MonoBehaviour
{

    public static int totalVehicleCounts;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (TrafficSystemController.road1_countVehicles)
        //{
        //    totalVehicleCounts = Sensor.vehicleCount + Sensor2.vehiclecount + Sensor3.vehiclecount;
        //    Debug.Log("TotalVehicles : " +  totalVehicleCounts);
        //}
    }
}
